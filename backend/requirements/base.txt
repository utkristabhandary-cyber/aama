Django==5.2.17
djangorestframework==3.18.0
psycopg2-binary==2.9.12
django-cors-headers==4.9.0
django-filter==24.3
python-dotenv>=1.0,<2
Pillow==11.3.0
openpyxl==3.1.5
# Production serving (Render web service — Linux). Gunicorn is the WSGI
# server used by the Render start command; WhiteNoise serves collected
# static files (/static/) with DEBUG=false where Django will not.
# Deliberately added for Render: local dev still uses `manage.py runserver`.
gunicorn==26.2.0
whitenoise==6.12.0